# 来源覆盖表｜AI时代，且慢如何存在与增长

报告使用稳定的 `section id / navLabel` 做来源映射，不依赖插入新章节后会变化的页面序号。当前共 42 个正文章节；原增长报告和 OAP 报告的实质内容全部保留。

## 本轮战略命题新增覆盖

| 输入 / 研究问题 | 当前报告去向 | 状态与边界 |
|---|---|---|
| 用户方法截图：“拆解到最小颗粒，重构为 AI 能力体系，再融入新时代入口、场景与工作流” | `future-form` / 存在形态 | 原句确认；战略含义为 `inferred`，不是生产完成证明 |
| 且慢不能只守 App，也不能成为匿名接口供应商 | `future-form` / 存在形态 | 中心判断为 `inferred`；四层形态为 `target` |
| App 在 AI 时代是否消失 | `app-agent-roles` / 双端角色 | Apple / Google 官方资料支持能力外露与 Agent 入口；不支持“App 消失” |
| 持牌责任、适当性、解释、授权与人工确认由谁承担 | `app-agent-roles`、`auth-governance` | 规则要求可定位；具体场景适用仍需法律与合规判断 |
| 外部入口如何变成增长，而不是匿名调用 | `growth-flywheel`、`growth-loop` | 飞轮为 `target`；当前归因链为 `missing` |
| 用什么指标判断且慢在增长 | `growth-metric-tree` / 指标树 | 月度可归因成功服务任务为 `target` 北极星 |

## 上一版 13 章覆盖

| 上一版章节 | 当前报告去向 | 处理 |
|---|---|---|
| 资源不变，入口先变 | `real-question` / 真正的题 | 完整保留双重不可能三角；开场判断降级为“入口结构正在变化” |
| 一条链路，两次跃迁 | `one-system` / 战略主线 | 升级为四段增长系统 |
| 小顾先接住真实问题 | `xiaogu-position` / 小顾定位 | 完整保留 |
| 从找功能到交任务 | `task-evidence` / 任务证据 | 完整保留两张脱敏生产图 |
| 23 种专卡生产落地 | `production-audit` / 生产实测 | 完整保留 35 / 23 / 5 / 7 与两轮测试边界 |
| 从会回答到完成任务 | `xiaogu-evolution` / 能力演进 | 完整保留 `confirmed / target` 边界 |
| 沉淀服务生产机制 | `service-mechanism` / 服务机制 | 完整保留 |
| 三次走出打开边界 | `three-leaps` / 三次跨越 | 完整保留原图 |
| 服务变成能力资产 | `capability-stack`—`system-map` | 用能力栈、递进、治理、鉴权与系统关系五章展开 |
| 把服务放进新入口 | `external-entrances`—`platform-coverage` | 增加微信、千问、百度与九平台状态矩阵 |
| 已证明能力，未证明增长 | `stage-evidence` / 阶段证据 | 完整保留并加入精确 OAP 数字与口径 |
| 破圈要形成经营闭环 | `growth-flywheel`、`growth-loop` | 新增经营飞轮图，原四步闭环完整保留 |
| 从能力可用到增长可证 | `growth-metric-tree`、`next-actions` | 新增指标树，原七项行动完整保留 |

## OAP 原 26 章覆盖

| OAP 原章节 | 当前报告去向 | 处理 |
|---|---|---|
| 年度目标 | `annual-goals` | 三项 KPI 与实验室目标完整保留；目标态不写成结果 |
| 关键举措 / 四个持续 / 行业认可 | `one-system`、`four-continuous`、`recognition` | 底座—持续—接入—群体关系及四项节点完整保留 |
| 用户增长 | `growth-journey`、`stage-evidence` | 502 日、19 个节点、四指标与双截止点完整保留；不依赖远程 iframe |
| 新流量入口 / 微信 / 千问 / 平台上架 | `external-entrances`—`platform-coverage` | 三类入口、两条联调路径、九平台状态完整保留并增强状态边界 |
| 五层能力 / 能力递进 / 能力治理 / 系统能力 / 系统关系 | `capability-stack`—`system-map` | 五层、三级、控制面、鉴权审计与原 SVG 完整保留 |
| AI 实验室 / 服务蓝图 / 服务创作 / 服务关系 | `ai-lab`—`service-map` | 一底座、一工厂、两终端、创作与反馈关系完整保留；未上线项标为 `target` |
| MCP TOP20 | `demand-signals`、`mcp-top20-appendix` | 需求结构摘要与完整 20 项榜单同时保留 |
| 金融机构 / 400+ 机构需求 | `institution-adoption`—`industry-opportunity` | 四类机构、八家样本、15 项工具榜与 Agent 需求完整保留；口径缺失单列 |
| 竞品分析 | `competitive-position` | 三类对比完整保留 |
| 商化准备 / 收费方案 | `monetization`、`pricing-design` | 需求、方案、运营与四象限路由保留；收入与价格状态分开 |
| 问题回顾 | `retro` | 技术、资源、用户、数据四类约束完整保留 |
| 下一步 | `next-actions` | 原四项行动全部进入七项行动 |

## 被主动降级或不外推的内容

- 20 万用户 / 300 万问题及 95% / 80% / 40% / 转化翻倍：仅进入开放问题。
- MCP 九平台矩阵：只说明源稿状态，不称为本轮生产实测。
- 400+ 机构：保留来源主张，同时明确周期、去重和活跃定义不足。
- 30+ 付费咨询：不解释为付费人数、订单或收入。
- 千问 / 微信联调：不解释为规模化获客、交易或 AUM。
- CAP 的 70% / 30%、1 小时—1 天与 AI 实验室目标：统一标为 `target`。
- Apple、Google 与 MCP 官方资料：只作为入口与协议趋势信号，不外推为中国零售投顾已经完成入口迁移。
- IOSCO、资管新规与证监会立法计划：用于风险结构与治理边界；本报告不构成法律意见。

## 未静默删除的内容

- 502 日增长口径、MCP 业务 TOP20、八家机构样本、机构工具 TOP15、收费设计与问题复盘均保留在正文。
- 原有 38 章及其状态标签全部保留；本轮新增 4 章，不覆盖旧报告或历史生产 URL。
