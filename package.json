{
  "name": "clair-ai-studio",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test tests/*.test.mjs && node scripts/validate-workbench.mjs && node scripts/validate-report-assets.mjs",
    "package:reports": "node scripts/package-recent-reports.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.1.5"
  }
}
